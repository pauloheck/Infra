apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-service-config
  namespace: beeai-prod
data:
  APP_ENV: "production"
  # kubelet identity client_id — necessário quando múltiplas user-assigned identities
  # existem no node pool. Diz ao DefaultAzureCredential qual usar via IMDS.
  AZURE_CLIENT_ID: "091d5b4b-4d60-4d44-a1e6-9dda63445900"
