###############################################################################
# NetworkPolicy — IAI namespace (dev)
# IAI é um serviço único com LoadBalancer direto (sem gateway).
###############################################################################

# Core: aceita tráfego externo (LoadBalancer) na porta 8000
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-external-to-core
  namespace: iai-dev
spec:
  podSelector:
    matchLabels:
      app: iai-core
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - protocol: TCP
          port: 8000

# Egress: pods podem fazer chamadas externas (Azure OpenAI, Key Vault, etc.)
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-egress-all
  namespace: iai-dev
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - {}
