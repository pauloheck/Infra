apiVersion: v1
kind: Service
metadata:
  name: beeai-web
  namespace: beeai
  labels:
    app: beeai-web
spec:
  type: ClusterIP
  selector:
    app: beeai-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
